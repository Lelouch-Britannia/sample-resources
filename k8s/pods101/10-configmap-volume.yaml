# ===== METADATA (Public - LearningUnit) =====
metadata:
  slug: "kubernetes-pod-configmap-volume"
  title: "ConfigMap Volume: Nginx + Prometheus"
  topic: "Pods"
  order_index: 10
  type: "coding"
  difficulty: "intermediate"
  
  description: |
    Create a Pod running Nginx web server and Prometheus monitoring with configuration injected via ConfigMap.
    
    **Focus**: Using ConfigMap volumes to inject complex configuration files (YAML) into containers without rebuilding images.
    
    This exercise demonstrates a realistic production pattern where monitoring tools (Prometheus) run alongside applications as sidecars. The Prometheus configuration is stored in a ConfigMap and mounted as a volume, allowing you to update monitoring configuration without changing container images.
    
    **The Architecture**:
    - **Nginx container**: Serves web traffic on port 80
    - **Prometheus container**: Monitors services and exports metrics on port 9090
    - **ConfigMap**: Contains `prometheus.yml` configuration file
    - **Volume mount**: ConfigMap mounted to `/etc/prometheus` in Prometheus container
    
    **Why This Pattern?**
    - Prometheus reads its config from `/etc/prometheus/prometheus.yml`
    - Storing config in ConfigMap allows updates without rebuilding images
    - Same Prometheus image can monitor different targets by changing ConfigMap
    - Both containers run in the same Pod for efficient monitoring
    
    **Configuration Structure**:
    ```yaml
    data:
      prometheus.yml: |
        scrape_configs:
        - job_name: 'my-service'
          static_configs:
          - targets: ['localhost:8080']
    ```
    
    When mounted, `prometheus.yml` becomes a file that Prometheus reads.
    
    **Real-World Use**: This pattern is used in service meshes, observability platforms, and microservices where monitoring sidecars need custom configuration.
  
  steps:
    - "Create ConfigMap named 'prometheus-configmap' with prometheus.yml data"
    - "Define scrape configuration for monitoring localhost:8080"
    - "Create Pod named 'webserver-prometheus' with two containers"
    - "Add nginx container exposing port 80"
    - "Add prometheus container exposing port 9090"
    - "Mount ConfigMap as volume to /etc/prometheus in prometheus container"
  
  hints:
    - "ConfigMap can be created in the same YAML file using '---' separator"
    - "Use configMap volume type to reference the ConfigMap"
    - "The prometheus container needs args to specify the config file location"
    - "ConfigMap data keys become filenames when mounted as volumes"

# ===== PUBLIC EDITOR CONFIG =====
editor_config:
  language: "yaml"
  initial_code: |
    apiVersion: v1
    kind: Pod
    metadata:
      name: webserver-prometheus
    spec:
      containers:
      - name: webserver
        image: nginx:latest
        ports:
        - containerPort: 80
      # TODO: Add prometheus container
      # - name: prometheus
      #   image: prom/prometheus:v2.30.3
      #   ports:
      #   - containerPort: 9090
      #   args:
      #   - --config.file=/etc/prometheus/prometheus.yml
      #   volumeMounts:
      #   - name: prometheus-config
      #     mountPath: /etc/prometheus
      
      # TODO: Add volumes section
      # volumes:
      # - name: prometheus-config
      #   configMap:
      #     name: prometheus-configmap
    
    ---
    # TODO: Create ConfigMap with Prometheus configuration
    # apiVersion: v1
    # kind: ConfigMap
    # metadata:
    #   name: prometheus-configmap
    # data:
    #   prometheus.yml: |
    #     scrape_configs:
    #     - job_name: 'my-service'
    #       static_configs:
    #       - targets: ['localhost:8080']

# ===== PRIVATE DATA (UnitSolution - NEVER exposed) =====
_solution:
  code_solution: |
    apiVersion: v1
    kind: Pod
    metadata:
      name: webserver-prometheus
    spec:
      containers:
      - name: webserver
        image: nginx:latest
        ports:
        - containerPort: 80
      - name: prometheus
        image: prom/prometheus:v2.30.3
        ports:
        - containerPort: 9090
        args:
        - --config.file=/etc/prometheus/prometheus.yml
        volumeMounts:
        - name: prometheus-config
          mountPath: /etc/prometheus
      volumes:
      - name: prometheus-config
        configMap:
          name: prometheus-configmap
    ---
    apiVersion: v1
    kind: ConfigMap
    metadata:
      name: prometheus-configmap
    data:
      prometheus.yml: |
        # Place your Prometheus configuration here
        # Example:
        scrape_configs:
        - job_name: 'my-service'
          static_configs:
          - targets: ['localhost:8080']
  
  validation_script: |
    #!/bin/bash
    # Apply the manifest (includes ConfigMap and Pod)
    kubectl apply -f solution.yaml
    
    # Verify ConfigMap was created
    kubectl get configmap prometheus-configmap > /dev/null 2>&1
    if [ $? -eq 0 ]; then
      echo "✓ ConfigMap created successfully"
      
      # Wait for pod to be ready
      timeout=90
      elapsed=0
      while [ $elapsed -lt $timeout ]; do
        ready=$(kubectl get pod webserver-prometheus -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}' 2>/dev/null || echo "False")
        if [ "$ready" = "True" ]; then
          echo "✓ Pod is running with both containers"
          
          # Verify prometheus container has the config file
          config_exists=$(kubectl exec webserver-prometheus -c prometheus -- ls /etc/prometheus/prometheus.yml 2>/dev/null)
          if [ -n "$config_exists" ]; then
            echo "✓ ConfigMap mounted to prometheus container"
            
            # Verify config content
            config_content=$(kubectl exec webserver-prometheus -c prometheus -- cat /etc/prometheus/prometheus.yml)
            if echo "$config_content" | grep -q "scrape_configs"; then
              echo "✓ Prometheus configuration is correct"
              
              # Verify prometheus is running
              sleep 10
              kubectl exec webserver-prometheus -c prometheus -- wget -q -O- localhost:9090/-/healthy > /dev/null 2>&1
              if [ $? -eq 0 ]; then
                echo "✓ Prometheus is healthy and running"
                exit 0
              fi
            fi
          fi
        fi
        sleep 3
        ((elapsed+=3))
      done
    fi
    
    echo "✗ ConfigMap volume validation failed"
    kubectl describe pod webserver-prometheus | tail -20
    exit 1
  
  quiz_answers: null
  quiz_explanations: null
